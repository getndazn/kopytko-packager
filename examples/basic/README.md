# Kopytko-packager basic example

## Setup
`npm ci`

## Usage
- Build: `npm run build` (result in /dist directory)
- Build & deploy: `npm start -- --rokuIP=<IP>`, example `npm start -- --rokuIP=192.168.3.2`
